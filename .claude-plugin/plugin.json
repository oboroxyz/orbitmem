{
  "name": "orbitmem",
  "version": "0.1.0",
  "description": "Skills for developing and extending the OrbitMem sovereign data layer — CLI commands, SDK layers, and relay services.",
  "author": {
    "name": "oboroxyz"
  },
  "keywords": ["orbitmem", "cli", "p2p", "vault", "agent", "web3"],
  "skills": [
    {
      "name": "orbitmem-cli",
      "path": ".claude/skills/orbitmem-cli",
      "description": "Develops and extends the OrbitMem CLI (npx orbitmem). Use when adding commands, modifying CLI behavior, debugging CLI issues, or working on packages/cli/."
    },
    {
      "name": "orbitmem-store",
      "path": ".claude/skills/orbitmem-store",
      "description": "Store data in an OrbitMem vault and register it on-chain via DataRegistry."
    },
    {
      "name": "orbitmem-discover",
      "path": ".claude/skills/orbitmem-discover",
      "description": "Search for data stored by agents in OrbitMem by tags, keywords, or quality score."
    },
    {
      "name": "orbitmem-rate",
      "path": ".claude/skills/orbitmem-rate",
      "description": "Rate data quality on-chain via OrbitMem FeedbackRegistry."
    }
  ]
}
